import type { NextPage } from "next";
import { Button } from "@mantine/core";

const Home: NextPage = () => {
  return (
    <>
      <Button variant="light" size="xl">
        test
      </Button>
    </>
  );
};

export default Home;
